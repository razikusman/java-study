package quizeTwentyFive;

import java.util.Arrays;

public class App {
    public static void main(String[] args) {

        // q1
        // String[] items = {"clock", "table", "ladder", "chair", "oven", "phone"};
        // String[] moreItems = items;
 
        // moreItems[2] = "sofa";

        
        // System.out.println(Arrays.toString(items)); 

        // output=>[clock, table, sofa, chair, oven, phone]

        // q2
        // String[] items = {"clock", "table", "ladder", "chair", "oven", "phone"};
 
        // String[] moreItems = new String[items.length];
        // moreItems[2] = "sofa";

        
        // System.out.println(Arrays.toString(items)); 
        // output=>[clock, table, ladder, chair, oven, phone]

        // q3
        // String[] items = {"clock", "table", "ladder", "chair", "oven", "phone"};
 
        // String[] moreItems = Arrays.copyOf(items, items.length);
        // items[2] = "sofa";

        // System.out.println(Arrays.toString(items)); //output=> [clock, table, sofa, chair, oven, phone]
        // System.out.println(Arrays.toString(moreItems)); //output=> [clock, table, ladder, chair, oven, phone]
    }
}
